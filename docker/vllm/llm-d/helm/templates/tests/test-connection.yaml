apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vllm-spnl.fullname" . }}-test-connection"
  labels:
    {{- include "vllm-spnl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vllm-spnl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
